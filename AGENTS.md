## 智能体技能

### Issue 跟踪器

Issue 在本仓库的 GitHub Issues 中跟踪。详见 `docs/agents/issue-tracker.md`。

### 项目管理记录

项目级进展记录在 Notion 中维护。每完成一个完整功能，都必须主动更新项目管理记录，再向用户报告功能完成。详见 `docs/agents/project-management.md`。

### Triage 标签

使用五个默认的规范 Triage 标签。详见 `docs/agents/triage-labels.md`。

### 领域文档

本仓库采用多上下文布局，通过 `CONTEXT.md` 与 `CONTEXT-MAP.md` 管理领域术语和上下文关系。阅读与维护约定见 `docs/agents/domain.md`。

### 前端设计文档

前端设计、实现、评审与验收前先读取 `web/DESIGN.md`，共享设计规则变化时同步维护。它与领域文档的分工、阅读和更新约定见 `docs/agents/design.md`。

### 架构沟通语言

解释架构时，先用具体业务场景说明代码职责、当前行为、风险和调整后的效果；使用架构术语时解释其含义。
