# Only Vite's development server reads these values. Do not use a VITE_ prefix
# for the token, or it will be exposed to browser code.
RCC_ADMIN_URL=http://127.0.0.1:8080
RCC_ADMIN_TOKEN=replace-with-the-admin-token
