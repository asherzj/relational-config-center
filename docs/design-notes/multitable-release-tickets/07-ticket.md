# T7：移除过渡结构并验收升级路径

状态：用户已确认切片及依赖；ready-for-agent。

## 父工单

Part of #81：https://github.com/asherzj/relational-config-center/issues/81

## 要构建什么

维护者通过新库初始化或已有结构升级得到唯一正式的数据和操作路径；用户只使用多表主单、明细与执行记录，不再遇到旧独立回滚单或旧整体JSON预算。完成所有临时结构的删除，并收口功能级证据。

## 验收标准

- [ ] 新库与已有表结构升级均运行真实验证；无旧发布单数据兼容，不自动清理环境业务或发布单数据。
- [ ] 删除T1/T3可能保留的单表默认字段、旧内部结果外观、仅服务整体JSON结果的结构和所有剩余旧回滚路径。
- [ ] 删除T5可能保留的旧确认流程字段/外观；无双写、兼容开关或新旧业务入口并存。
- [ ] 核对命令、通知、版本、权限和接口调用方全部采用正式归属，不靠旧结构继续执行。
- [ ] 逐项核对AC-001～AC-015已有交付证据，必要跨工单接线回归通过；不得以本工单代替各工单自己的测试。
- [ ] 领域词汇、ADR、接口与Web设计文档一致，完成Notion项目管理更新后报告功能完成；不自动关闭历史父规格。

## 验收用例

唯一主要交付：AC-015。采用父规格Given/When/Then；其他相关用例是必要的回归和跨切片证据，不重复分配主要责任。

## 验证证据

真实MySQL初始化/升级和旧结构检查证明部署与删除结果；针对最后删除内容执行API及浏览器接线回归；验收索引关联各工单独立证据。

## 临时结构与退出条件

本工单是明确的收缩/删除责任工单。临时结构必须全部删除；若发现新增过渡结构，先在其引入工单和本工单登记，禁止留下无责任项。最终有未解决阻塞或用例无证据不能声明完成。

## 阻塞于

- #85（T4）
- #86（T5）
- #87（T6）

## 交付要求

独立分支和可写worktree，一张工单一个实现、验证、评审、提交、推送及关闭周期。按仓库当前Go/Web检查和设计规则执行；用户未要求并行，默认按可执行前沿串行。

## 执行分配

使用全新上下文的独立subagent，模型 gpt-6-astra，思考强度 high，在专属分支和可写worktree实现本工单。读取父规格、完整工单及评论和最新领域/设计文档，按implement流程完成必要验证、双轴评审、提交、推送和工单关闭周期。用户已确认此交付周期；禁止直接推送或合并main，不强制推送。模型选择不增加业务范围；本期默认串行，优先留出双轴评审所需并发槽位。
