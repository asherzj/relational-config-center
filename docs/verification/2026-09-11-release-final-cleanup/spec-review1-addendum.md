应追加 **P2：仍存在可执行的缺表名兼容路径**。

- `draft_details.go:40–43` 为已有明细的增量更新补表名。
- `release_orders.go:849–855` 接受复制/重新准备缺表名，并为旧调用方回填。

两者绕过正式的逐项表名必填验证，违反 AC-015/T7 的兼容退出要求。删除回退并补公开 HTTP 负例是合适的修复；保留复制时按位置绑定原 `detail_id` 不受影响。本轮未编辑文件。
