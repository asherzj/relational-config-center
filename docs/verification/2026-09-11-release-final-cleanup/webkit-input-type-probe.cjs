const {webkit}=require('/private/tmp/rcc-issue-88-final-release-cleanup/web/node_modules/playwright');
(async()=>{const browser=await webkit.launch({headless:true});try{const page=await browser.newPage();await page.setContent('<input aria-label="single"/><textarea aria-label="multi"></textarea>');const expected='宽'.repeat(3<<20);for(const label of ['single','multi']){const input=page.getByLabel(label);await input.fill(expected);const value=await input.inputValue();console.log(JSON.stringify({label,...await input.evaluate(e=>({tag:e.tagName,type:e.type,maxLength:e.maxLength})),characters:value.length,bytes:Buffer.byteLength(value),equal:value===expected}));}}finally{await browser.close()}})().catch(e=>{console.error(e);process.exitCode=1});
