# Spec

**状态：通过。Findings：0。**

规格原句：AC-017「100可提交，101在Web提示且Admin仍拒绝；分页、请求体等现有边界不被取消」。冻结 snapshot（manifest SHA-256 `db1b08ec…d6f4`）与五个路径哈希一致。

- `CombinedQueryForm.test.tsx:98-111` 仍逐项操作真实 UI：第 1 项以 `findByRole` 获取；第 2～100 项每次点击缓存但 DOM 身份稳定的“添加集合值”按钮，以完整、精确的 `aria-label` 定位新节点，断言其为 `HTMLInputElement` 后写入 `1`～`100`。组件只收集非空输入，随后断言提交数组长度为 100，足以证明 100 个实际输入进入提交值。
- 第 101 项仍用 `getByRole` 校验可访问名称并写入 `101`；既有 `validateQueryDraft` 在调用 `onSubmit` 前拒绝超过 100 项。错误文案断言和 `submit` 仅调用一次的断言均保留，证明第二次查询未提交。
- diff 仅替换测试定位方式并增加节点类型断言；未改生产代码、20 秒边界、容量、错误文案或既有断言，没有 scope creep、临时兼容或可疑行为。Admin 的 101 拒绝及分页/请求体边界已有基点 `ea368013…` 的真实 HTTP/MySQL 证据；本次无生产变化，无需重复运行数据库。

**证据：** 原 Linux CI 为 479/480，通过项之外唯一 AC-017 在 20 秒边界超时（报告耗时 28.283 秒）；修复后 AC-017 JSON 1/1 通过（1.582 秒），同文件 13/13 通过且 AC-017 为 1.383 秒。提交前相关复验充分。

**Unresolved / 待补证：** push 后 PR 自动 Linux Web 全套仍待运行；这是最终 CI 结果待补，不是本地修复验证缺失。
