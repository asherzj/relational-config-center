# #110 WebKit 1.63 comparison — Standards review

**Object:** base `a221562036136942ff5cbbeace95a44d13532900` → tree `70579231454628eb5dacdd3c1ffb3a752d297e01` (32 changed paths). Fixed-tree review used the repository Standards sources and all twelve Fowler smell checks.

**Hard standards violations: 0.** `.github/workflows/ci.yml:166-220` changes only the PR-111 temporary diagnostic job. `scripts/diagnose-accounts-webkit-version.sh:1-53` verifies the original package/lock hashes before mutation, uses a task-specific `mktemp` backup, applies the frozen reviewed patch, requires a frozen install, and asserts Playwright/core `1.63.0`, WebKit revision `2359`, and browser version `26.6` before invoking the unchanged signal supervisor and business runner. The EXIT trap restores both files, rechecks original SHA-256 values, removes only its named backup, preserves any existing nonzero status, and converts cleanup failure after success to status 1. The candidate patch changes only Playwright/playwright-core and removes their orphaned Darwin-only `fsevents@2.3.2`; all other dependency graph entries remain equal. Installed candidate packages persist only inside the disposable job. Logged artifacts contain dependency hashes/diff, versions, executable location, cleanup state, and the pre-bounded signal output; this delta adds no argv/environment, file-content, core, credential, or permission capture.

**Subjective Fowler smells: 0.** The version supervisor is bounded apparatus with one responsibility and a mandatory removal condition. The fixture avoids dynamic resolution, so no speculative abstraction or duplicated dependency-selection logic warrants a finding.

**Evidence/gates:** `reviewed-manifest.json` SHA-256 is `11ddfdafeec0a292627cde586fd78b13c9216c4ae928a13734c9535b687944bb`; all 36 reviewed entries and all 29 `SHA256SUMS` entries match tree bytes. Formal package/lock, eight jobs, accounts, runner, repeat generator, and signal supervisor match the base. Five restoration/exit/corrupt-candidate cases pass. The local evidence identifies installed Node packages only; the native 1.63 comparison remains **PENDING**. This passes the local gate for one bounded evidence commit, but does not establish a product/crash fix. Final delivery remains blocked until the result is interpreted and the temporary job, three diagnostic scripts, generated runner, active fixture/backup, installed comparison graph, wrapper, and DEBUG instrumentation are actually absent, followed by the original required CI.
