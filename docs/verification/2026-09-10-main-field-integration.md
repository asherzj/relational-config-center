# main 字段交互与多表发布集成

本地集成基点 `f341a458b8313aa865f3bf0cf96fda131977bea3`（#81 的 T1～T4），接入 main 的字段交互 MR #90，提交 `4aeb54ed8664d569555a3800f9a3c21994b1e890`。本记录仅证明两套已验证能力的新增接线；#86 的错误与重推、#87 原因历史、#88 清理和全功能交付仍待完成。本次不推送或创建 MR。

## 合并结果

- 保留 main 已发布的字段规则迁移 014 与 ADR-0024；本功能未发布的原单执行迁移改为 015，草稿占用迁移改为 016，多表决策改为 ADR-0025。迁移内容保持，源码、就绪提示、升级文档和真实升级测试使用新编号。历史验证文件中的旧编号描述的是当时切片版本。
- 保留配置控件、组合查询、Schema／会话恢复、字段配置和完整实际 before/final；多表草稿去向、IndexedDB 原请求日志、全局明细顺序及各表版本／通知继续有效。API 同时保留字段错误定位和目标占用者信息。
- 发布单审阅按每条明细／实际命令所属表展示当前字段与选项标签，同一视图按不同表各读取一次。列表隐藏设置不隐藏申请或实际结果，不从当前业务值重构历史。并发更新的最新草稿与记录预览各自读取其表集合，另一窗口首次加入新表也能正确审阅。
- 独立的开发／测试 `release-reset` 在原有显式目标和停写保护下，将明细、成功执行、表引用与其他发布历史共八张表放在同一事务清理；保留业务值、Record Version、Table Version 和 Command Cursor。仅在本轮新建的隔离测试库执行该维护工具，未对既有环境执行清理。

## 验证

| 范围 | 结果与证据 |
| --- | --- |
| Admin 普通测试及编译 | `go test ./...` 与 `go build ./...` PASS；覆盖公共接口和现有架构检查。 |
| 真实 MySQL 受影响集成 | 11/11 PASS：旧结构升级／新库相等、缺失就绪提示、字段迁移保留数据、字段原子保存、数据库默认值和发布边界、三项重置安全／中断／续发、多表冻结顺序及实际结果、全表保护锁、多表重新准备竞争。 |
| Web 受影响回归 | 241 项有效 PASS：首轮其他 15 文件 182 项，最终发布单 59 项。新增同名字段不同表、申请／发布／恢复结果及并发 A→A+B 最新预览标签验证。 |
| Web 类型与生产构建 | 最终 `pnpm build` PASS。 |
| 真实浏览器 | 账号父路径及 draft-targets、release-multitable、field-recovery、field-display 四个脚本 PASS。多表脚本含八项检查，保留大正文响应丢失、账号隔离、原键重推和存储故障证据；本轮增加按实际表显示标签，390px 通过真实方向键横滚核对实际 final。 |
| 静态检查 | `node --check`、相关 Go 文件 gofmt 与 `git diff --check` PASS。 |

本轮数据库验收串行，同一时刻最多一个本任务 MySQL 实例，保留用例内部多连接／并发屏障。普通 Go 与前端构建缓存可复用；未为这次合并重复完整后端／Web 套件，功能收尾由协调者集中运行最终完整套件。

日志哈希、11 个后端测试名、浏览器来源和关键源码哈希见 [机器证据](2026-09-10-main-field-integration/evidence.json)。人工已查看 [390px 键盘横滚后的真实恢复值](2026-09-10-main-field-integration/multitable-restoration-mobile-final.png)；[桌面实际结果](2026-09-10-main-field-integration/multitable-publication-desktop.png)保留全局明细、实际所属表和 before/final。

## 保留的失败与复验

首轮 Web 238/239 PASS，唯一失败是旧“表内变更顺序”可访问名称断言未随全局明细标识更新，已修正并在最终 59 项中通过。新增多表标签用例首次暴露 Provider 依赖旧主单摘要，改为实际明细表集合后通过；首次新用例用了 Testing Library 不支持的 `exact` 类型参数，删除该测试参数后类型／构建通过。依赖安装首次因非交互 CI 标志缺失、Go 首次因沙箱构建缓存权限、一次 Node 检查因目录路径错误未运行；正确环境／路径下已分别执行通过，不将 setup 失败记成业务失败或通过。

首轮浏览器 draft-targets、field-recovery、field-display 通过；release-multitable 的当前标签断言通过后，旧手机截图断言未将新增实际对比表横滚到 final 列而失败。补入真实 ArrowRight 操作及 scrollLeft 检查，保留原值完整可见和页面无溢出断言；复验账号父路径、draft-targets 与 release-multitable 全部通过。原始失败日志保留，不改记为通过。

## 独立评审

Spec 首轮发现最新草稿预览漏读并发新增表的字段标签，已修正两处预览 Provider，并以 A→A+B、中文字段／选项、一次读取及输入保留回归复核，最终 0 项。Standards 初审发现迁移说明旧编号、重置历史职责说明、ADR 显示编号和重复表集合映射，已修正文档并以同一表达式提取 `releaseDetailTables`；最终复核为 0 项，源码与日志哈希已按提取后的文件刷新并核验一致。纯提取后再跑发布单 59 项及类型／构建通过；因行为、依赖及环境未变，复用此前浏览器通过证据。
