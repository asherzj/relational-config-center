# #110 OS-signal diagnostic — Standards review

**Review object:** base `490a9e18017dff934c27a74dee0db3193a03fb8a` → tree `2c906bed92aaa90c58095ff1b4ebd857af8a6838` (37 changed paths). I read all changed source and evidence from the fixed tree, plus base `AGENTS.md`, `docs/agents/design.md`, `web/DESIGN.md`, `docs/agents/domain.md`, and `CONTEXT-MAP.md`.

**Hard standards violations: 0.** `.github/workflows/ci.yml:166-222` adds a PR-111-only diagnostic without changing the original eight jobs. `scripts/diagnose-accounts-webkit-signals.sh:1-41` confines strace to the Playwright WebKit launcher tree and the stated exit/wait/kill syscalls plus signal records; it does not trace exec, argv/environment, reads/writes, stacks, or memory dumps. Its EXIT trap restores and verifies launcher bytes and mode, preserves an existing runner failure, and turns cleanup failure after success into failure. The outer workflow trap removes the generated runner. The formal runner and `accounts.mjs` hashes match the base. These implementation details do not change Web design or domain language, so the documentation-maintenance rules in `docs/agents/design.md` and `docs/agents/domain.md` do not require updates.

**Subjective Fowler smells: 0.** I checked all twelve baseline smells. The temporary generator/wrapper are narrow, explicitly bounded diagnostic apparatus with stated removal conditions; they do not justify a smell finding.

**Evidence and unresolved:** `reviewed-manifest.json` SHA-256 is `6865af46a44d1a72d55f97bd0138ec3b829836d294c2c63fca1e8a5e558d37b4`; all 37 entries match tree bytes. All 33 `SHA256SUMS` entries also match. Local controlled evidence covers renderer following, SIGABRT sender/wait status, argv/environment canaries, exit 42 preservation, and restoration success/failure cases. The native amd64 business probe remains **PENDING**; this delta may pass the local gate for one bounded evidence commit, but it does not establish a crash fix or final delivery. Final delivery remains blocked until the native result is interpreted and the temporary job, both scripts, generated runner/cache wrapper, and DEBUG instrumentation are actually removed, followed by the required affected regression/CI review.
