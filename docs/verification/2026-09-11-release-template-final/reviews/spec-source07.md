Spec source06→source07：新增0项，未解决源码问题0项；manifest 与 diff 匹配。

历史结构比较固定正式版本5与 Contract+016 边界一致，完整签名保留；current11等价由独立测试承担。accounts 明确等待原POST200及确认窗口关闭再比较两次原键原包；目标字段与选中表等待避免切换中操作；mutation 两处仅补齐显式正向配置。完整 SQL、12 Go Browser、Compose和最终索引仍待补齐，暂不整体放行。
