source04 增量 Spec 复核：0 项新发现，完整验收结果仍待补证。manifest 与 diff SHA256 均匹配。

- 4 处 DROP 改为 RENAME，仍使正式表名不可读取；503、启动拒绝与维护原子性断言未变，符合 AC-023/026。
- 3 处清理仅删除原有临时表规则所关联的子记录，范围与既有清理谓词一致；未修改正式业务接口或放宽外键。
- 两份设计文档与统一阶段、真实实例展示一致，符合 AC-025/027。

Notion 是 root 在提交、推送及远端核验后的完成钩子；当前不视为提交前阻塞，也不宣称已完成。最终整体完成仍须 root 更新、读回并核验。
