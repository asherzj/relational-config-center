Spec source04→source06：0 项发现。manifest 与 diff 摘要匹配。

夹具 helper 仅由指定验收场景调用，经正式 API 补缺并保留已有关联；没有运行时默认流程兜底。operation-coverage、complex-fields 先注册合法账号，再加载夹具和显式关联，未放宽 ready 或授权。business_auth 三处、mutation 五处仅补齐正向发布前置配置；原授权、故障、事务及历史断言保持不变。

完整 MySQL、浏览器、Compose、视觉和最终索引仍待补证，不作最终放行。
