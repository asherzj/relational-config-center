# Issue #105 verification

Scope: AC-019 / AC-020 / AC-021 / AC-024 of #100, implemented on `codex/issue-105-emergency-rollback` from fixed base `d31eabd6f7947004d39fe872afee8aaca37bbad2`. This record was finalized for the pre-commit delivery review after all required verification completed; its source snapshots intentionally precede the issue commit. #106 owns removal of the old ROLLED_BACK overview and feature-wide final verification; neither is claimed here.

## Implementation and fixed source

The original order stores `rollback_table_flows` in its existing JSON document. Explicit preview POST saves current per-table EMERGENCY templates only on the first save, advances the original version once, and records a typed original preview result in the existing request journal. GET remains read-only. Subsequent new-key previews reuse saved instances; original-key replay returns the original preview even after completion or rollback. Actual execution still rechecks current authorization, original state/version, original retained targets, current business values and frozen execution schema through the shared transaction. It ends the original order directly, records only true execution actors, stops unused completion nodes, releases targets and saves participant notifications atomically.

The Web journals the preview request before sending, does not automatically resend an unknown result after reopening or refreshing, and offers explicit save/rebuild controls in the shared rollback window. A generic conflict read sends GET only. Historical preview acknowledgement does not replace the authoritative current header or enable another rollback. Original execution key/body/reason survive until explicit execution rebuild. Historical previews now identify the current ended state in the dialog's top description, before old instance nodes.

Final current source is `reviews/source-07.json` / `reviews/source-07.diff`, SHA256 `8b71cdd313012daeb91e1aa98c0a5027150876f6c7d636963cd3bb6756808ced`. It includes all 45 changed/new source and specification paths, with an independent full source copy at `/private/tmp/rcc-105-review-snapshot-07`. All previous review inputs and failed logs remain unchanged. Subsequent delivery manifests bind the final evidence separately.

No migration, SQL fixture, SchemaMaintainer, schema registry or dependency lock changed. Official migrations 1–9 and candidate migrations 10/11 are unchanged. The cumulative real-MySQL schema evidence from the fixed integration baseline (`../2026-09-11-template-final-integration/schema/`) remains applicable to the unchanged 43-case schema boundary; this issue additionally ran the two affected HTTP-composition/current-schema-entry boundary tests. No verification ledger or historical data was edited.

## Acceptance mapping

| AC | Public behavior and critical boundaries | Evidence |
| --- | --- | --- |
| AC-019 | Explicit first preview saves two independent current emergency instances on the original order before display; original forward flows unchanged; version advances once; template/association later changes cannot replace saved nodes; new-key reopen and exact old-key replay; empty arrays and saved instances appear in write responses, details, ordinary list and notification list; GET cannot write. | `03-preview-red.log` → `04-preview-green.log`; enhanced `18-narrow-corrections.jsonl`, `19-public-arrays-boundaries.jsonl`; `17-public-empty-flows-red.log`; browser 01 contract failure → browser 04 successful full chain; final historical-copy browser10 supplement below. |
| AC-020 | Whole original order restores current values, ends directly ROLLED_BACK, releases ownership; optional reason can be filled/corrected afterward; saved publication nodes record the real actor/time while completion nodes stop without invented human completion; completed/rolled-back orders cannot execute again. | `05-result-nodes-red.log` → `06-result-nodes-green.log`; `14-affected-http.jsonl` mixed/unique/FK/audit/time/capacity/result-replay cases; browser 04 empty-reason keyboard execution, two-table restoration, true terminal nodes and later reason editing. |
| AC-021 | Real historical approvers and applicant receive transactional result notifications; actor's existing unread remains unchanged; emergency draft cancellation sends no result, submitted emergency cancellation uses true SUBMIT and creates no false approvals/pending/handled work. | `07-emergency-cancel-red.log` → `08-emergency-cancel-green.log`; `19-public-arrays-boundaries.jsonl`; `14-affected-http.jsonl` notification participant, failure, unknown-commit, lost-response, terminal-race and disabled-account cases; browser 04 applicant notification jump to original detail. |
| AC-024 | First preview source/instance/result storage failure rolls back nodes/version/history/request result; original key can safely retry after recovery; current authorization is rechecked after transaction lock even on replay; account-scoped keys cannot import another actor's receipt; actual execution rejects later business changes and terminal races without partial effects or duplicate success/reminders. | `09-current-auth-red.log` → current-authorization four-subcase green in `10-auth-failures-green.log` and `14-affected-http.jsonl`; all three preview storage faults green in 14; `18-narrow-corrections.jsonl` existing execution failure invariants; `24-preview-identity.jsonl`; Web unknown/rebuild and browser 04 original-byte recovery after completion/rollback. |

The preview uses original-order request/version consistency requirements shared with AC-023, but AC-023's main ownership remains with #104. This report does not double-claim that AC.

## HTTP and MySQL results

`http-outcomes.json` is the machine-readable top-level test index. The latest applicable outcomes contain **42 unique top-level tests, all PASS**: 39 from the affected HTTP scope, two schema-composition boundaries and one additional current-account preview case. Go subtests and package events are counted separately.

- `14-affected-http.jsonl`: 39 top-level cases = 33 PASS / 6 FAIL; 45 subcases = 35 PASS / 10 FAIL. This batch remains an original failed batch, not an all-green run.
- `18-narrow-corrections.jsonl`: 7 top-level PASS, 13 subcases PASS. It covers every failed top-level case from 14 plus enhanced AC-019. The six old failures were version/history expectations after the new preview-save event or comparison of differently authorized projections. Their original conflict, ownership and transaction-failure assertions remain; baselines now use the same authenticated reader and post-preview version.
- `19-public-arrays-boundaries.jsonl`: 3 top-level PASS (AC-021 response arrays plus two schema entry boundaries).
- `24-preview-identity.jsonl`: 1 top-level PASS (non-publisher cross-account replay denied; another publisher's same key cannot recover an old-version outcome; role revocation denies original replay; restoring permission recovers the same receipt without changing the order).
- `20-package-contracts-final.jsonl`: 3 packages PASS, 28 top-level and 77 subcases PASS (`internal/domain`, `internal/application`, `internal/interfaces/http`).

The later backend change after 14 was response-array normalization and preservation of the same saved-flow field in read projections; 18/19 verify those public paths. The later identity addition changes only a test. Thus the unaffected business-transaction PASS results from 14 remain applicable; the six failures have explicit newer green counterparts rather than being discarded or rerun indiscriminately.

Actual inverse ordering is proven by `TestMultitablePublicationPreservesGlobalOrderAndOriginalResults` (PASS, 10.99s, a real `a_release_children` → `z_release_parents` foreign key) and `TestReleaseRollbackUnwindsUniqueValueDependencies` (PASS, 19.77s, DELETE/MODIFY unique-value dependencies) in 14. Browser command `sequence` is a **per-table** cursor. The browser checks each table's cursor and table version advance exactly once plus actual restored rows; it does not infer transaction order from cross-table cursor comparison or a client-side reversed list.

## Red/green and intermediate failures retained

Backend logs 01/02 are fixture-setup failures (mutation policy and duplicate association), preceding the real missing-persistence red in 03. Logs 11/12 preserve intermediate caller-adaptation compile errors; 13 compiled successfully. The filename `10-auth-failures-green.log` does not mean the whole batch passed: current authorization and non-freezing checks passed, while three storage tests compared different readers and failed; all three are green in 14 after using the same reader. Log 23 preserves a supplementary identity-test fixture mistake: the applicant had PUBLISHER, so the correct response was a stale-version conflict rather than the intended permission denial; 24 explicitly sets EDITOR before asserting denial.

The Web helper's `web/verification.md` indexes every raw red/green and intermediate failure. The complete affected nine-file scope passed 179 tests in `24-conflict-affected-regression.log`; the final recovery-gate change passed 14 selected cases in `30-conflict-final-recovery-regression.log`. The historical top-description adjustment passed 27 selected recovery/rollback/instance cases in `34-history-context-regression.log`. The subsequent focus correction passed the same 27-case affected scope in `37-preview-focus-regression.log`; typecheck/build passed in final logs 38/39. No artificial red was introduced for inherited behavior that already passed.

## Real Chrome and visual evidence

Every browser run uses installed Chrome through the repository runner, a fresh output directory, a same-origin Vite proxy, an actual Admin executable and its own MySQL 8.4 Testcontainers instance. Existing preview-admin/mysql containers are not used or changed. The browser scripts register temporary actors and perform business operations via public HTTP/UI. Raw trace ZIPs are native Playwright artifacts, not postprocessed or manually compressed logs.

- Browser 01 / backend16: real contract failure on `rollback_table_flows:null`, before the service response fix. This failed run is not delivery evidence and source changed during its verification period.
- Browser 02 / backend21: real chain reached saved original-preview recovery; layout capture sampled a finite CSS entrance/viewport animation. The later failure screenshot fits the viewport. Raw geometry/screenshots remain; script now waits for finite animation completion without relaxing bounds.
- Browser 03 / backend22: layout and actual rollback succeeded; a script incorrectly compared independent table cursors. The script now asserts each table's own cursor/version changes. An initial progress explanation incorrectly called the field an original detail sequence; this paragraph records the corrected contract.
- Browser 04 / backend25: complete `TestRollbackFlowBrowserSystemPath` PASS, 37.30s. Source04 hashes matched before/after (`browser/04/source-binding.json`). Evidence contains 22 desktop/390px screenshots, corresponding layout JSON, exact original preview/execute requests, actual returned snapshots, and two native traces. Covers saved preview response loss → close/reload → manual same-key/body/pre-save-version acknowledgement, template modification and fresh-key reopen isolation, empty-reason whole result, later reason editing, applicant notification navigation, and historical preview acknowledgement after both COMPLETED and ROLLED_BACK.
- Backend26: affected account browser setup failed before its business subpaths because its required output directory did not exist; the next attempt uses a fresh precreated directory. No product claim comes from this failure.

Visual inspection of browser04 used original-resolution desktop preview and notification detail, 390px preview/unknown-result/reason/notification list and terminal-history screenshots. Long Chinese template/node/title strings wrap within 390px; dialogs keep fixed reachable footer actions with the body scrolling inside; optional reason, saved current nodes and keyboard focus are legible. The old ROLLED_BACK overview remains #106's scope. Root inspection identified that the historical-state explanation sat below the scrolling diff; the final source moves a specific historical/current-ended-state description to the top of that dialog; browser10 below verifies it with current screenshots.

## Commands and applicability

Database/browser commands use `TESTCONTAINERS_RYUK_DISABLED=true DOCKER_HOST=unix:///Users/asher/.colima/default/docker.sock GOCACHE=/private/tmp/rcc-go-cache`; tests run with `-count=1`. Backend14 runs `go test -tags=integration ./cmd/admin -run '^(TestRollbackFlow|TestQuickRollback|TestOriginalOrderRollback|TestReleaseRollback|TestRollbackReason|TestReleaseNotifications|TestReleaseCompletionPermanentlyClosesRollbackWindow|TestReleaseEmergencyAC017AC018|TestMultitablePublication|TestMultitableThousand|TestMultitableLarge|TestPagedExecution|TestReleaseThousandItems|TestReleaseHistorySurvives)' -count=1 -json` from admin. Backend18 selects its seven named tests recorded in the top-level outcome index; 19 selects AC021 and the two named boundaries; 24 selects the new identity case.

The new browser target is `go -C admin test -tags=integration,browser ./cmd/admin -run '^TestRollbackFlowBrowserSystemPath$' -count=1 -v`. Affected old browser coverage selects only `^TestAccountBrowserSystemPath$/(release-rollbacks|release-rollback-reason)[.]cjs$`, plus the independent `TestReleaseNotificationsBrowserSystemPath`; the account harness necessarily executes its account setup prerequisite, which is reported separately from the two selected subpaths. This is not the full repository or final feature acceptance suite.

## Independent review and delivery gate

Initial independent Standards found one P2 (generic read secretly persisted preview); Spec found one P1 (null array) plus failed/pending verification. All initial reports remain in `reviews/`. The latest independent reports bind a fixed source snapshot and retain separate findings/counts. Both final axes now bind source07: Standards has 0 written-standard violations and 0 independent subjective smells; Spec has 0 unresolved findings and no pending required evidence. The coordinating agent retains the commit/push and project-record completion gate.


## Final affected-browser supplements

Backend27 / browser06 retained the old account harness's association-version setup failure for `release-rollbacks.cjs`, while the independent `release-rollback-reason.cjs` subpath PASS (6.00s) is applicable: executor-as-VIEWER keyboard reason editing and administrator original-key/body recovery after response loss. Public GET now reads the association version before explicit STANDARD configuration; readiness remains required.

Backend28 / browser07 then exposed an actual focus regression: initial async preview saving disabled the preferred cancel control, and focus stayed on the modal container. The original focus assertion was retained. The local fix restores cancel focus when pending ends only if focus is still on the container/body, preserving a user's move into the reason or another control. Backend29 / browser08 PASS: `TestAccountBrowserSystemPath` 56.17s, selected `release-rollbacks.cjs` 17.99s. It verifies initial cancel focus, desktop/390px preview, no restoration on cancel, original execution key/body/digest after response loss and reload, real current values, result-view switching and competing completion. Its old result-history expectation now includes exactly one preview-save event. The script's legacy phrase “cancels without writes” specifically asserts zero quick-rollback execution requests and unchanged business data; preview persistence is separately expected.

Account setup runs necessarily precede selected child scripts; repeated setup runs are not distinct acceptance paths. The isolated fixture containers are stopped and removed by their owners after each run. `schema-unchanged.json` independently compares all 80 existing schema-related/SQL source paths with the fixed base; all are unchanged.


Backend30 / browser09 PASS: `TestReleaseNotificationsBrowserSystemPath` 43.22s. Publication, completion, rollback and reprepare each survive committed-response loss and original UI/key/body replay; real applicant and former approvers receive results, actor exclusion preserves existing unread, two tables restore once, and 14 keyboard/list/detail acknowledgements complete with `errors: []`. Source07 hashes matched after the run.

Backend31 / browser10 PASS: `TestRollbackFlowBrowserSystemPath` 35.30s on the final source07, including the entire new recovery path and updated historical top descriptions. `source-binding.json` verifies all 45 source hashes and no source drift. Both 390px terminal screenshots visibly say “这是原请求已保存的历史恢复预览。原单已完结/已回滚，不能用于新的回滚。” before historical ACTIVE nodes; cancel has the safe focus ring, execution is disabled. All 22 final desktop/mobile layout records pass unchanged viewport/dialog bounds, and `errors: []`. Main implementation and the independent Spec reviewer separately inspected these original-resolution screenshots.

The latest applicable browser evidence covers three distinct Go browser targets: the new rollback-flow target, the result-notification target, and the account target with two selected business subpaths. Equivalently, four requested business browser scripts plus their required account setup script ran successfully. Repeated attempts, failure screenshots and individual assertions do not inflate this unique-path count. Reason evidence from27 remains applicable after the final QuickRollbackDialog-only focus/text correction; its separate reason editor and shared transport were unchanged.

The main implementer released the exclusive DB slot after31; each owned Testcontainers fixture was stopped and removed. The temporary `web/node_modules` symlink is removed before delivery; its external dependency target is untouched. All failure logs, JSONL, native traces, screenshots and review snapshots are retained without trimming. The unchanged inherited verification directories are not part of this issue's modifications.
