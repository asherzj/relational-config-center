# Spec initial review — #105

状态：存在阻塞发现，且必需证据待补。已完整读取 #105 / #100 正文、评论（均无）与 ready-for-agent 标签；范围仅 AC-019/020/021/024 及其适用一致性要求。

输入：基点 `d31eabd6f7947004d39fe872afee8aaca37bbad2`，HEAD 同基点、commit 列表为空；43 路径 `source-input.json` 与包含新增文件的 `source-input.diff`，SHA256 `664dd688ec4a979a607c4472a203e29e561392c3c48904f28675423ab4eb007c`。审查绑定此快照，后续修改需复核。

- **(a/e) P1：未预览原单的公开响应与 Web 契约不一致。** AC-019 要求“打开恢复预览并查看流程”，主入口为正式 Web。`web/src/api/release-orders.ts:37` 对新增 `rollback_table_flows` 强制数组；`admin/internal/application/release_orders.go:170` 创建原单未初始化该切片，HTTP 写响应 `releaseResponse` 与详情 `releaseHeaderResponse` 也未归一化，因此尚未预览的真实原单返回 `null`。新建/提交等写响应及详情都会触发 Web `contract_mismatch`，恢复入口无法正常到达。需使正式响应与数组契约一致，并取得真实 HTTP→Web 验证；仅给 mock 加空数组不能证明修复。

- **(b) AC-024 受影响回归尚未通过。** `backend/14-affected-http.jsonl` 已结束为 FAIL：完结版本、重启历史预览操作者、分页结果版本、外部变更/目标保护及事务故障后的历史断言仍失败。本票新增实例保存三类故障、四条当前授权竞争、真实节点/通知及终态测试已绿；这些失败目前应归为待完成的回归复核，不能仅据失败旧断言推断生产事务错误。需保留失败证据并补对应修改版本的绿结果。

- **(b) AC-019/020/021 浏览器证据待补。** `web/verification.md` 明确脚本仅语法检查。需实际结果证明桌面/390px 预览、丢响应→关窗/刷新→同账号原包原键原版本手动恢复、直接结束/原因补填/通知跳转，以及旧成功预览不倒退后来完结或回滚的主单。脚本与平台边界测试不是这项证据。

**(c/d) 未发现范围蔓延或本票替换分支退出遗漏。** 源码已保存逐表独立应急实例；普通详情、普通列表和通知列表保留实例投影；执行前再核当前权限、状态与业务值；回滚发布节点取真实执行事件、完结节点 STOPPED；历史参与人通知与业务同事务保存，已提交应急取消使用真实 SUBMIT 判断。#106 的旧 ROLLED_BACK 概览退出与全量收尾不列为本票问题。

计数：1 项代码阻塞，2 项待补证；不运行 DB、浏览器或测试，不修改生产源码。
