# 当前字段展示 #78 验收

基点 `c91b3ac`，规格 #71、工单 #78。继承已验收 #74～#77，沿用 `web/DESIGN.md`。本工单只改变展示读取与页面表达，不改变查询／编辑配置冻结、发布权限、执行 Schema、发布语义快照或回滚。

## 验收接缝与证据

| 用例 | 证据 |
| --- | --- |
| AC-013 | `ManagedDataPage.test.tsx` 验证结果列按当前 `is_visible` 过滤，再按 `display_order`、真实字段名排序；重复中文名和重复选项名称均同时显示真实字段／实际值。删除确认和 `ChangeSetDialog.test.tsx` 验证隐藏字段仍完整展示。页面按表共享一次字段配置读取，组合查询只冻结首次成功配置，列表的实时读取不会重绘已打开查询／编辑控件 |
| AC-014 | `ReleaseOrdersPage.test.tsx` 验证申请差异、实际 before/final 使用当前中文名与选项名称且忽略列表隐藏；修改配置后历史显示更新，原始值不变；同表不同发布单导航重新读取配置。503 时保留原字段和值、错误代码／请求编号和独立重试。`PublicationResult` 只遍历持久化 command 的 before/final，不读取当前业务行 |
| 重复标签与回退 | 当前显示共享模块始终保留真实字段名和实际值；无配置时直接回退原名原值。浏览器以同一个选项名称配置 before/final 两个不同值，实际结果仍可分别核对；随后改名并重新打开，页面换成新名称 |
| 系统证据 | `field-display.cjs` 使用独立 EDITOR/PUBLISHER、APPROVER、ADMIN 账号，经真实 Admin/MySQL 创建、审批、发布并读取详情。配置 `name.is_visible=false` 后实际结果与申请仍展示该字段；配置改名后重新 GET 发布单，断言序列化 before/final 与改名前完全相同。真实 503 保留原始历史，解除故障后页面内重试恢复当前标签 |

## 先红后绿

- 列表验收先因仍按数据库列顺序、隐藏列仍出现、值只显示原码而失败；接入按表读取的当前展示模块后转绿。
- 删除确认最初只显示记录编号，测试要求展开完整记录且包含列表隐藏字段后转绿。
- Change Set 的中文字段和选项名称用例先失败；共享同一展示规则后，字段／选项名称与真实值同时出现。
- 发布详情最初没有实际 before/final 字段表，也不会随当前配置更新；改为只渲染持久化 publication command 后转绿。
- 同表不同发布单切换用例验证详情重新挂载并读取当前规则，防止只依赖表名的旧缓存名称复用。
- 浏览器首轮仅因错误回退时断言了未格式化文本失败；页面实际保留的是持久化 `值：原值`。修正验收定位后同一系统路径完整通过，产品代码无需为测试调整。

## 验证结果

- 完整 Web：`pnpm --dir web test:run --maxWorkers=2`，34 个文件、374 项测试通过，54.24 秒。
- `pnpm --dir web build`：TypeScript 与生产构建通过。
- `make test`：Admin、Client、Server、Shared 单元测试与架构检查通过；Admin 包 21.700 秒。
- 真实浏览器：`TestAccountBrowserSystemPath/field-display.cjs` 及必跑 accounts 主路径通过，最终轮 57.30 秒，字段子路径 3.78 秒；Chrome 152、1440×1000 与 390×844，四项检查及空错误列表见 `result.json`。评审修订后另跑 `combined-query.cjs` 与 `field-recovery.cjs`：组合查询通过；字段恢复更新实时列表新增的一次读取边界后，最终轮 42.08 秒、子路径 6.69 秒通过。完整三引擎与跨工单联验由 #80 汇总。
- `git diff --check` 与 TypeScript 项目检查通过。

本机集成测试使用隔离 Colima/MySQL、localhost NO_PROXY 和本地 TCP 权限，运行结束销毁容器。原始日志、失败轮截图及重复 accounts 证据保存在 `/private/tmp/rcc-78-browser-raw`；本目录仅保存最终检查清单和两张精选截图。浏览器 profile、Cookie 与共享 `node_modules` 链接不提交。

## 实际视觉核对

1. 桌面申请差异中“最新名称”与真实字段 `name` 同组展示，原始名称／目标名称下方分别保留真实值；隐藏配置没有删掉审阅字段。
2. 实际发布结果逐字段并列展示实际 before/final，当前标签只承担辅助阅读，原值仍用等宽文字核对。
3. 重复标签场景由自动化先验证两个同名标签及两个不同原值，再更新为不同标签采集最终图。
4. 390px 页面没有文档横向溢出；宽字段表在自己的滚动容器内保留完整内容，发布操作与视图切换仍可达。
5. 中性灰画布、石墨操作、浅红原值和浅绿申请值沿用现有设计规则，没有新增展示专用色或并行组件体系。

已增量维护 `web/DESIGN.md` 的列表、删除确认、Change Set 和发布历史展示规则。ADR-0024 已定义实时投影与持久化快照边界，本工单没有新增领域概念或持久化字段，因此 CONTEXT／ADR 无需变更。
