# 发布模板、应急发布及正式审批通知依赖集成

本目录记录接入 #104 的最终依赖验证。固定本方检查点为 `e7e19b022fc606c137fdf8df1899caf9a5702bb1`，已正常合入 #98/main `eb55e841e80345cfef7733b8c560d6037256cd2c`；本次正常合并已交付 #104 `f545adf24aadd34ee39afb2643a4819aee54c986`。此前 #97 和 #98 的原始验证分别保存在相邻目录，不能把它们的通过结果冒充本轮最终版本。

## 合并职责

- 保留通知的一致读取快照及当前用户通知字段；常规列表与通知列表构造审批读取对象时均保留发布方式、应急原因、保存的逐表实例及缺配置表。共享筛选校验纳入 #104 的真实待发布状态。
- 发布单摘要同时提供当前通知与应急原因；历史原请求结果继续不承载当前通知。完整保留 #97 原请求失败、当前授权和真实参与人通知的测试断言。
- 新版紧凑详情页保留页头操作、收起的基本信息、人员 ID 展开、差异内容及通知已读；接回应急原因、实际阶段和逐表实例，不伪造审批事实。
- 正式迁移 1～9 和本方已经验证的候选 10/11 不因接入 #104 改变。此前 43 个唯一 schema 验收的复用需根据本轮生产代码及实际调用影响核对。

## 验证与边界

后端 52 个唯一真实 HTTP/MySQL 验收已全部有效通过：初批 51 PASS/1 FAIL，单个历史重启夹具经逐行 SQL 诊断与精确新增请求校验后窄补验 PASS，原批仍如实保留为 exit 1。四 Go 包和 integration/browser 编译通过。Web 6 文件 154 项相交回归、类型检查、构建及六脚本语法检查通过。

实际浏览器初批六路径中，实例、应急、通知中心、发布通知、审批通知五条 PASS；account 容器内的审批详情子路径被 late SQL 测试夹具缺少四表应急关联导致的 schema_not_ready 拦截，06a 用真实 SQL 和公开 readiness 503 复现；07 在原夹具后经正式 PUT 建立四表的两类关联，公开 readiness 200，审批子路径 9 个检查全部 PASS。最终六条路径有效通过，前五复用原结果；原 06/06a 失败不改写。所有初批代码输入未在运行中改变，六个自有容器均已结束，工作流 harness 记录的六个 PID 也已消失。原始失败、诊断、窄复验及源码输入保留；不能把首次六条记为全绿。

Root 已核实 backend 23 份证据哈希与 52 个实际顶层 PASS；两父提交 1,472 / 1,256 份已有 verification Git blob 均原字节保留。正式 1～9 的 18 个 SQL/manifest 与已发布 #98 一致，全部 22 个当前迁移文件未因 #104 改动；此前 43 个 schema 验收仅复用于未改变的迁移/ready边界，不替代本轮业务或浏览器结果。

本轮仅做依赖接入。#105 仍负责原单应急回滚模板、应急取消结果通知等新增验收；#106 负责旧回滚总览退出及全功能最终回归。部分新增读取路径因共享接口合并已具备能力，#105 应据实验证，不人为制造失败。

## 验证适用范围与交付索引

最终新增改动只有 account 浏览器 harness 的显式配置夹具：没有其他调用者，不改变 HTTP 或前五个浏览器路径，不改变迁移 SQL/manifest、生产就绪检查或历史 SQL。最终该 harness 已实际编译并完成目标子路径；完整 account 套件由 #106 的全功能回归执行。默认 installed Chrome channel，独立 MySQL 8.4 / Admin / Vite。并未据本轮六条路径宣称全仓浏览器通过。

浏览器的首批命令、每批输入、真实退出和容器检查在 `browser/`；诊断和补验分别为 06a/07。视觉核对见 `browser/visual-verification.md` 和独立 Standards 报告。后端有效索引在 `backend/effective-results.json`，Web 命令及初次失败在 `web/README.md`。最终源/证据哈希与精确交付文件清单位于本目录。独立 Standards/Spec 报告保存在 `reviews/`；此前过程目录继续保留当时状态，不倒改历史记录。

最终独立 Standards / Spec 对本轮集成增量均为 0 项未解决发现；Standards 发现的三段 DESIGN 遗漏已恢复并复核。此放行只覆盖依赖集成，不代表 #105/#106 已完成。
