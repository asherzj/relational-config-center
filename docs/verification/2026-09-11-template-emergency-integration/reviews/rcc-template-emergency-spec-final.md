# Spec：#104 接入模板、紧凑详情与通知的最终增量复核

结论：**本次集成增量范围内 0 项未解决规格缺陷。** 六条唯一浏览器路径形成有效 PASS；这是本轮接入的复核结论，不代表 #100 全部功能、#105 或 #106 已交付。

固定基点 `e7e19b022fc606c137fdf8df1899caf9a5702bb1`，incoming `f545adf24aadd34ee39afb2643a4819aee54c986`；工作树 `/private/tmp/rcc-template-notification-integration`。封存时 HEAD/MERGE_HEAD 与上述两点一致。沿用已经完整读取的 #100/#104 与 #98 正文、评论、标签及 AGENTS、领域、ADR、发布契约和 DESIGN，只做接入增量的 Spec 复核。未读 Standards 报告，未运行数据库，未修改实现。

## 合并后的业务语义

- 发布列表与通知中心的两个投影同时保留应急原因、发布类型、逐表实例和缺配置事实；个人通知字段也保留。列表仍使用一致快照读取，共用筛选验证接纳真实 `PENDING_PUBLICATION`，没有把待发布合成审批通过。
- AC-012/015/016：整单切换与全部参与表的新实例同事务保存；应急原因只属于提交输入，必填及长度验证保持。应急没有审批安排、审批历史或自动发布，人工执行仍校验当前发布权限。
- AC-017/018/023：跨表一起发布，保留占用至人工完结；保存及末端故障整体回退，原请求结果可恢复。复制/重新准备建立当前配置的新实例，不继承已完成审批。既有通知故障测试没有被较旧 #104 夹具替换。
- 紧凑页头、折叠基本信息、当前主动作、更多操作、人员 ID 展开与个人已读保留。应急直接显示“待手动发布”，逐表流程读取保存实例；原因可在基本信息和提交历史核对，不显示虚构审批事实。
- 恢复组件保持 #104 已修复的审阅门禁：每次检查先撤销提交建包资格，完整最新读取、明细分页、预览、当前提交资格与基线检查通过后才能建包。明确冲突后的新包跟随最新类型；读取失败或未知结果保留原身份、正文、版本、键与原因。中性成功反馈不会以原提交历史结果覆盖当前状态。
- 常规审批及发布通知的事务、持久历史、故障回退和已读语义保留。当前角色 bit 4 退出及不可变历史解释没有回退。新增应急通知/回滚模板属于 #105，临时回滚总览属于 #106，不作为本增量缺陷。

## 实际证据与适用范围

后端 `01-http-matrix` 为 51 PASS / 1 FAIL，原失败保留。历史测试经 `04` 独立复现，`05` SQL 诊断证明旧请求原字节不变，仅多出一次合法停用表配置请求；`06` 窄修后 PASS。修正仅排除并独立核验精确账号、操作和请求键对应的那一行，其他旧行仍逐字节比较，后续重启比较也包含这行。生产代码未变，形成 **52 个唯一顶层 HTTP 测试有效通过**，并非一次完整批次全绿。四个契约包通过；integration/browser 编译检查通过只代表编译。

受影响 Web 六文件 **154 项 PASS**，类型检查与构建通过。最初通知测试夹具缺少新增响应字段的失败保留；补入空 `emergency_reason` 没有修改业务断言。六个本轮调整的 E2E 脚本语法通过。表策略启用显式使用当前版本并配置关联；实例入口与真实逐表拒绝者定位跟随合并后的布局，拒绝者永久 ID、业务结果及实例断言没有放松。DESIGN 恢复 incoming 已交付应急约定后，Web 实现和测试未变。

已有迁移矩阵为 **43 个唯一顶层有效 PASS**。正式 SQL/manifest、迁移/就绪实现、这 43 项测试及迁移夹具均未改变，可复用迁移兼容性结论。其最终 251 项输入与当前不同的 11 项分别属于应急业务实现、调用输入及局部浏览器/历史测试；已经逐项核对影响，不以旧迁移矩阵代替本次 HTTP 或浏览器验证。最后 account 浏览器夹具修正只有一个调用者，未改变迁移测试路径。

浏览器 `01`～`05` 和 `07` 均为真实 Chrome → Vite → Admin → 独立 MySQL 路径，exit 0，共 **六条唯一路径有效 PASS**：实例、应急、通知中心、发布通知、审批通知，以及 account 主流程中的真实 `release-approvals.cjs`。不把 account 父测试和子测试重复计数。

证据覆盖两表独立流程、显式修复缺配置、部分/全部审批、应急整单切换、实际提交丢响应后的原包重推、同账户恢复、另窗手工发布、原提交重放仍显示当前已发布、完结、通知已读/撤权/计数失败/跨账户延迟响应，以及既有常规生命周期通知。`07` 另核对真实拒绝人、复制及重新准备、未知结果原包恢复、审批后取消与持久业务值。已独立查看桌面部分审批、390px 应急待发布/原因对话框、通知详情、常规已审批详情及重新准备结果截图，节点、原因和当前状态一致，长标题正常换行。

初次 `06` 在 account 主流程后注入历史 SQL，再运行 `release-approvals.cjs` 时，维护 CLI 被 `schema_not_ready` 拦住，整体 exit 1。`06a` 诊断证明四张 late fixture 表已启用却缺少关联。最终只修改 account 测试准备：沿用原管理员会话，经正式 PUT 为固定四表显式建立 EMERGENCY/STANDARD 关联，使用期望版本 0 和逐表/类型独立请求键，再断言 public ready 200。两份历史 SQL 逐字节不变；没有降低生产就绪检查或伪造迁移账本。`07` 实际创建八个版本 1 关联、ready 200，父测试及目标子路径均 PASS，容器已清理。

最终 `07` 的 480 项输入全部匹配。前五条与当前仅 account 专用测试准备文件不同，其路径没有调用该准备函数，因此保留其 PASS；52 项 HTTP 及迁移结论同样未受该局部改动影响。所有原始失败、诊断和窄补验分别保留。

实际输入清单：`/tmp/rcc-template-emergency-spec-final.inputhash.json`，包含 658 项源码和 176 项证据哈希及逐次适用性比较。SHA-256：`eec3b354bac06a13b3f10e7154ff8cf33462c20ba723f422e61235b511599c9b`。
