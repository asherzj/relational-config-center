# Account browser late-fixture readiness repair

Original `06-release-approvals` failed at a fixture account's maintenance `grant-admin` with `schema_not_ready`, after the real `accounts.mjs` journey had passed. Its log, metadata and source snapshot are preserved. No production defect was found.

The harness applies `release-rollbacks.sql` and `field-display.sql` after current migrations and public policy setup. These historical fixtures directly insert four enabled Table Policies without Table Release Templates. #102's readiness invariant requires every enabled table's valid EMERGENCY association. The exact missing rows were `field_display_browser_items`, `stage1_rollback_chromium_items`, `stage1_rollback_firefox_items`, and `stage1_rollback_webkit_items`; all had enabled=1 and association_id=NULL. The original SQL creates business fixture tables, not control DDL. No ledger operation occurs here.

`06a-fixture-diagnosis` ran the same account setup with a nonmatching management subtest selector, using one new MySQL 8.4 Testcontainer. A read-only SQL LEFT JOIN captured all four missing rows, and public `GET /health/ready` returned 503. The added setup readiness assertion failed as intended (exit 1, 44.19 seconds). Raw SQL values are byte arrays in the preserved Go log, following the existing full-column snapshot helper.

The only source change is `admin/cmd/admin/account_browser_integration_test.go`. The existing setup helper returns its authenticated administrator cookies/CSRF to the caller. After the historical SQL, the harness explicitly PUTs each of those four tables' EMERGENCY association to `default_emergency_v1` and STANDARD association to `default_standard_v1`, enabled=true, expected_version=0, with a distinct durable request key per table/type. The emergency associations satisfy the required contract; standard associations explicitly preserve those publication/rollback fixtures' intended ordinary publication path. No implicit repair or fallback was added. The fixture still requires public readiness 200 before any management browser journey. Both historical SQL files remain byte-identical. Production readiness, migrations, ledger, history and Web scripts are unchanged.

`07-release-approvals` ran only `^TestAccountBrowserSystemPath$/^release-approvals\.cjs$` with `-tags=integration,browser -count=1 -v -timeout=8m`. The harness's required accounts journey ran first; the requested approval journey passed all nine checks (people lookup fallback, frozen non-self-approval, reject/copy, lost approval original-key replay, partial MODIFY display, saved flows/390px/keyboard, pre-dispatch reprepare recovery, atomic replacement original-key replay, cancellation/history/business-data preservation). Eight association responses and readiness 200 are in the raw log. Actual exit 0, duration 69.44 seconds, source_changed empty. There are 13 screenshots including the accounts prerequisite and five release-approvals screenshots. No first-five browser path or full suite was repeated.

Both runs used new disposable `rcc_test` databases in `mysql:8.4`, newly built local Admin/account-maintain binaries, loopback Vite/Admin, and the existing installed Chrome default. Environment: `TESTCONTAINERS_RYUK_DISABLED=true`, `DOCKER_HOST=unix:///Users/asher/.colima/default/docker.sock`, `GOCACHE=/private/tmp/rcc-go-cache`; exact commands/output paths, input hashes and container checks are in each run's JSON. The runners are preserved here. No preexisting preview container was used or changed. New containers were terminated and absent on exact inspect; cleanup is recorded in `cleanup.txt`. DB was returned to root. No stage or commit was performed.

The first five passing browser results are still applicable: this change touches only the account harness setup, and no production or Web source changed. `source-applicability.json` verifies that only this file changed between original 06 and final 07, all 07 input hashes still match, and both SQL fixtures remain original. Root owns final visual review, integration review and delivery gate.
