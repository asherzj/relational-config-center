# 依赖集成与迁移编号

原实现/首轮验收基点为 `3351112689ffb68f11d2dc3cceb17a48910a74cb`，未发布角色迁移临时编号 00004。#88 发布 00004/00005 后，主协调者给出已验证集成基点 `bd3e5d16389d7e82dca54ea022dafcb99b993778`。本张在保留 stash `f843d2198f93e10526b519fb7bc4dc0081ca590c` 后快进接入，仅恢复本张改动。依赖既有证据见上级 `2026-09-11-approval-dependency-integration.md`。

冲突逐项解决：控制表保护名单取并集；Web 设计保留新发布详情规则并追加角色管理；当前测试版本改为 6；迁移和运维文档保留历史 014～017 升级链。00004 manifest 恢复已发布原内容，角色 SQL 顺延为 00006。未读取或接入 #88 未提交代码。

#88 已发布未接管历史边界为版本 5；本张使边界显式冻结在 5。原实现冻结 3 的未发布调整没有带入新基线。接管核查历史完整结构并登记 1～5，随后 pending；显式 up 6 后 current。恢复已有尝试使用原目标版本及摘要。冻结历史 SQL/数据和 00001～00005 SQL/manifest 均不改写。

00006 manifest 在本任务专用 MySQL 8.4 容器新数据库 `rcc_issue93_v6` 顺序执行 1～6 SQL 后，通过 `mysql --default-character-set=utf8mb4 -NBr` 的 SHOW CREATE TABLE 生成。包含 24 张完整控制表，逐项断言 00005 全部定义未变。新角色四表为空；未改写已有业务数据。
