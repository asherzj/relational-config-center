# #104 Spec 最终复核

结论：**本工单审查范围内 0 项未解决规格缺陷。** Web 初审的恢复门禁 P2 已修复，原发现与复核分别保留在 `/tmp/rcc-104-spec-web-round1.md`、`/tmp/rcc-104-spec-web-round2.md`。这不是 #100 完整功能或最终集成的交付结论。

固定基点 `e2564842e72e96b6ffb1e86708978b11dd8a651b`；工作树 `/private/tmp/rcc-issue-104-emergency-release`；按授权覆盖该点至当前工作区的 34 个生产、测试、设计/契约变更。完整读取 #100/#104 正文、评论、标签及领域、ADR、发布契约、DESIGN；只做 Spec，未读取 Standards 报告，未运行数据库或修改实现。

- AC-012：整单类型切换与逐表新实例同事务保存；界面保留方式，丢响应仅手动重推原包。提交冲突重建跟随最新类型，完整审阅失败不能建包。
- AC-015/016：有效原因必填，真实待手动发布，无审批安排、审批事实或自动发布；当前权限在事务内核对。成功提示没有把历史原包结果描述为当前状态。
- AC-017/018：跨表整单执行、末端故障全回退；真实发布后保留占用至完结，完结保持配置/版本并释放全部占用。
- AC-023：依赖/保存失败不伪装缺配置；已保存实例保持稳定，复制/重新准备建立新实例。未知结果、刷新及同账户会话中断后保留身份、正文和原因，明确成功前没有成功提示。

后端沿用已封存 round1 并核对最终原包补验，有效 38 项真实 HTTP PASS。Web 最终相交 108 项 PASS（42），6 项恢复针对性 PASS（41）；类型检查、构建通过且源码哈希完全匹配。此前有效集合 429 项加本轮 4 项新用例为 433 项，属于保留失败后补验的有效覆盖，非一次全量全绿。

02/03 真实浏览器链路为浏览器→Vite→Admin→独立 MySQL：两表方式切换、实际已提交后丢响应、原因校验、会话恢复、另窗手动发布、原提交重放仍显示当前已发布、完结及占用释放均 PASS。已查看桌面与 390px 移动截图。03 后只改明确拒绝的恢复组件/测试，该增量由 41/42 覆盖。

#105 的通知和回滚模板接入不据此报缺陷；最终合并后的迁移及交互回归仍由集成门禁负责。

实际输入清单：`/tmp/rcc-104-spec-final.inputhash.json`；SHA-256：`e4686e7bda4f02828e6e6506c68560067333a48cd7a0d58f2aceec68d94b7c21`。
