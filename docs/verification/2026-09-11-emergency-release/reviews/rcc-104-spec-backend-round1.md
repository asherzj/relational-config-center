# #104 Spec 后端初审

结论：**未发现需修复的后端规格偏差（0 项）**。本轮不代表整票验收完成。

固定基点：`e2564842e72e96b6ffb1e86708978b11dd8a651b`。HEAD 尚未新增提交，按明确授权审查该基点至工作区的变更。输入为 v2 的 16 文件；其中浏览器 harness 随后增加 `RCC_E2E_ISOLATED=1`，已只读核对并记录实际哈希，其余 15 文件与 v2 一致。独立读取 #100/#104 全文、评论与标签（#104 用 gh 实时读取，评论为空），核对 AGENTS、领域、相关 ADR 及发布契约。未读取 Standards 报告，未运行数据库或修改源码。

| 规格 | 代码与实际证据 |
| --- | --- |
| AC-012 | 方式与各表实例整体替换，同内容、版本、目标及原请求结果同事务；两种竞争次序、过时拒绝见 02、09、12 日志。 |
| AC-015 | 原因必填，真实待发布，不写业务或制造审批事实；04、15 日志。专用提交输入接受原因，执行/完结/恢复预览拒绝该字段。 |
| AC-016 | 事务内重读当前权限；有权限的申请人可手动发布，撤权后原包重放拒绝；06、15 日志。 |
| AC-017/018 | 整单执行保持跨表顺序、末端故障全回退、真实版本/结果、待完结占用；完结不写业务并释放占用、关闭回滚；07、15 日志。 |
| AC-023 | 模板读取和实例/结果持久化故障不伪装缺配置；失败保持原状态，成功原包重放不替换实例；08、12 日志。实际丢响应与界面输入恢复留待本票浏览器增量审查。 |

12 回归原始 exit 1 保留：两项旧夹具错误等待旧锁、比较实时审批上下文。14 独立复现后，v2 改用真实 HTTP body gate 与精确授权锁等待，并仅将审批上下文与当前 GET 比较；原业务、历史、原包及原子性断言保留。15 六项窄补验全部 PASS、exit 0；16 全 Admin integration/browser 编译 PASS。本轮未另跑测试。

后续只审 Web/浏览器证据及新增修改；#105 通知与原单回滚模板接入不作为本票缺失。未发现新增兼容、自动重试或旧流程兜底。

输入哈希：`215e39799c0504f7e636c51a79dbad5f3081ef4c15db4eebaf393a59f4ebe269`；清单：`/tmp/rcc-104-spec-backend-round1.inputhash.json`。
