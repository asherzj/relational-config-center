# 待批准的 #104 真实浏览器验收

状态：自动审批两次拒绝创建含认证写入的浏览器脚本；该脚本未创建，也未执行。仅有后端验收和 Web 组件测试通过，真实浏览器部分仍待批准。此文件是审阅说明，不包含脚本或替代执行入口。

## 拟执行目标和启动来源

使用当前 #104 独立工作树 `/private/tmp/rcc-issue-104-emergency-release` 的 `TestReleaseEmergencyBrowserSystemPath`，由既有 `runReleaseWorkflowBrowserSystemPath` 启动：

1. `admin/cmd/admin/discovery_integration_test.go` 的 `startCurrentIntegrationMySQL` 调用 `startIntegrationMySQL`，通过 Testcontainers 创建新的 `mysql:8.4` 容器，使用容器生成的连接信息；库名为 `rcc_test`。只对该新建库运行本构建 Goose 迁移和 `testdata/003-policy-fixture.sql`，生成测试表 `policy_alpha`、`policy_beta`。测试清理使用 `TerminateContainer` 删除本次容器。
2. `admin/cmd/admin/release_flow_browser_integration_test.go` 编译本工作树 Admin，用上述新容器的连接配置启动临时本机进程；注册固定测试账号 `template.browser.admin`，用同库账号维护工具授予 ADMIN。Vite 在动态分配的 `127.0.0.1` 端口提供同源页面并代理该 Admin。
3. 拟浏览器脚本仅在显式隔离标识且目标 URL 为 loopback 时接受运行；浏览器使用新上下文，不使用用户现有浏览器账号或 Cookie。输出为本票证据目录下全新运行子目录，旧文件不得覆盖。进程和容器由测试清理。

这些是拟采用的隔离措施，不能将其写成自动审批已经认可。当前尚未创建这次浏览器运行的容器，因此没有可声称已核对的目标容器 ID、端口或数据库连接。得到授权并启动时须记录实际容器/本机进程/输出位置，再运行写入场景。

## 认证写入范围

浏览器通过正式接口在该一次性库中建立测试查询规则与变更规则，启用两张测试表；创建一份三节点常规模板和一份两节点应急模板，并分别设置两张测试表的两类关联；已有同类型多模板能力不改动。随后创建、编辑和提交测试发布单，执行两表配置记录变更，人工完结并再次创建目标竞争单。相关原请求记录、版本、流程实例、执行记录、目标占用和刷新通知记录都属于这次测试库。

此外会通过测试账号会话的退出/重新登录及浏览器传输层丢响应验证输入保护和原请求恢复。不会接入用户的现有数据库、已有部署、正式账号、外部通知收件人或 Server/Client；不部署应用、不运行外部正式迁移、不推送或合并分支。

## 拟验证步骤和成功标准

- 创建包含两表 ADD 的应急草稿，测试记录 ID 为 `10401`、`10402`，读取长模板名和已保存节点；通过正式 UI 改为常规，模拟服务端已提交但丢响应，刷新后手工原包重推；再通过 UI 切回应急，核对两表实例整体重建。
- 在保存错误、旧版本竞争或响应丢失后保留当前窗口输入；刷新与同账号恢复后手动点击原业务按钮，核对原键、原路径、原版本和完整正文。请求结果成功时使用正常详情读取当前状态。
- 在 390px 下填写应急原因：空白或 2,001 个 emoji 拒绝且不发写请求，合法原因提交模拟丢响应并进入真实“待发布”；公开业务查询证明提交本身没有生效。页面无审批人、批准事实或虚假审批节点。
- 用当前发布权限手动执行整单。两表实际结果、真实节点人员和时间可见，成功后等待人工完结并保留目标保护；竞争单修改相同目标被拒绝，回滚入口仍可见。
- 人工完结后业务值不再变化、目标释放，新的两表 MODIFY 草稿可保存但不执行；原单不再允许回滚。历史原请求重放不把当前详情倒退。
- 桌面与 390px 检查键盘焦点、标签、长名称换行、认证遮罩、未保存退出及输入保护、页面无横向溢出；生成原始截图与逐项浏览器检查结果并逐张视觉核对。

## 自动审批拒绝的风险说明

第二次拒绝明确指出：脚本包含大量认证写入、启用规则、发布和业务数据变更；仅凭环境变量、loopback 及隔离标识不足以证明精确目标可丢弃或授权这些副作用。拒绝禁止通过替代入口、间接执行或规避策略达到相同结果。已停止脚本创建和执行，等待在说明风险后获得明确授权；其余验证继续进行。
