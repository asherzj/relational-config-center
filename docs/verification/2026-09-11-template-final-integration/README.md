# 发布模板与正式审批权限、通知依赖集成（进行中）

本目录记录最终依赖集成过程，尚未完成验证或交付。起点为本地中间检查点 `aec58942f01143c1a579aaf2a885141bf5a263c9`，它已合并 #103 与 #97；原局部证据在 `../2026-09-11-template-notification-integration/`。目前正常合并实际已发布、已进入 main 的 #98 提交 `eb55e841e80345cfef7733b8c560d6037256cd2c`；之后接入 #104 已交付提交 `f545adf24aadd34ee39afb2643a4819aee54c986`。不会合入 #98 未公开原始档案提交。

#98 发布时汇总了历史，Git 的共同祖先早于实际双方已经拥有的 #97。对冲突文件分别读取本地检查点、已核验 #97（7334e1103f612b4735992ef63bf1a0b7643b40ae）及已发布 #98 的真实内容，进行逐文件三方合并；仍有冲突的部分按双方语义手工解决，记录见本目录 JSON。保留通知序号、真实历史参与人、原请求结果、模板与逐表实例、新版详情布局及旧 APPROVER 当前授权退出。

## 当前实际验证

- 正式 1～9 的全部 18 个 SQL/manifest 与 #98 原字节一致。未部署模板 SQL 仅顺排为 10/11。`schema/01-generate.log` 在全新 MySQL 8.4 上执行正式前缀，再依次执行候选 SQL；从每张表的实际 SHOW CREATE TABLE 生成 10（28 表）和 11（29 表）累计清单，测试 PASS、包 14.281s、exit 0，容器已结束。生成器源归档于 `schema/manifest-generator.go.txt`，临时测试源已删除。
- `backend/01-contracts.log` 为 domain/application/MySQL/HTTP 四包实际测试 PASS；命令和源输入另存，不包含真实 MySQL 验收。
- 受影响 schema、cutover、历史接管与新装等价矩阵最终 43 个唯一顶层 PASS。原 02 运行 35 PASS/7 FAIL，03 对四份历史夹具修复和新增前缀恢复用例运行 12/12 PASS，有效合并 43；原失败字节保留。当前全部 Admin 输入与 03 一致。详见 `schema/README.md`。
- Web 合并后 154 项相交回归、类型检查、构建和 E2E 语法检查通过。两轴对本次 #98 接入增量均无遗留；报告及原输入归档于 `reviews/`。浏览器与接入 #104 后的最终验证仍待。

## 待完成

完成 #98 合并检查点并接入 #104；覆盖受影响 HTTP、Web、浏览器相交路径，核对当前迁移升级/恢复数据保留与就绪条件；独立双轴复核、全部输入和证据清单、索引及提交核验。#105 新原单应急回滚/通知语义和 #106 全功能收尾均未开始，本目录不是这些工单的完成证据。
