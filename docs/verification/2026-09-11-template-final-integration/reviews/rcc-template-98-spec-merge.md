# Spec：#98/main 接入增量

结论：**当前合并内容未发现确定的规格缺陷（0 项）。** 本报告只覆盖接入增量，数据库验证仍在收敛；不表示最终集成或 #100 完整功能已经交付。

## 输入与范围

工作树 `/private/tmp/rcc-template-notification-integration`；HEAD `aec58942f01143c1a579aaf2a885141bf5a263c9`，MERGE_HEAD `eb55e841e80345cfef7733b8c560d6037256cd2c`。核对以已包含双方实际共有内容的 #97 `7334e1103f612b4735992ef63bf1a0b7643b40ae` 做逐文件三方合并的记录及当前人工结果；index 仍处于合并中。

已实时完整读取 #98 正文、评论和标签，结合已读 #100/#103/#97、领域/ADR、正式迁移与发布契约、紧凑详情设计及当前 DESIGN。未读 Standards 报告，未修改源码或运行数据库，不重复评审 #98 已交付的原功能。

## 合并语义

- 紧凑业务标题、折叠基本信息、危险“更多操作”、人员 ID 展开及审阅工具栏保留。逐表节点仍读取已保存实例；缺配置仍有显式保存补齐入口，未恢复常规固定流程兜底。当前权限、已保留原请求、写入互斥及取消确认继续控制页头动作。
- 发布应用/领域、两类读取投影、HTTP DTO、通知事务和接线、Web API、ReleaseFlows、ReleaseNotificationRead 共 10 处关键文件逐字节等于 pre98 HEAD。页头仍依据这次实际展示的个人通知进度确认已读，紧凑布局未移除通知处理。
- 当前角色 bit 4 退出，其他位号固定；不可变历史使用独立解释类型。迁移 9 的合法旧值恢复边界与后续 10/11 校验没有混用。正式 1～9 的全部 18 个 SQL/manifest 与 incoming 逐字节相同；候选 SQL 9/10→10/11 仅文件编号变化。
- 夹具将 8→9 收缩与 9→11 模板增量分别验证。62 组旧角色/启停组合明确断言映射及角色版本；快照排除的仅为允许变化的当前角色字段，历史、会话、责任、通知和业务继续比较。模板升级比较保留既有数据；真实 HTTP 审批事实复制到正式 v8 结构，不伪造旧账本。旧版本就绪用该测试独占空库重新安装真实 v1，随后观察运行中 Admin，未降低业务数据保留断言。

## 实际证据与限度

四个 Go 包 PASS，124 项输入哈希匹配。Web 8 文件 154 项 PASS，类型检查/构建及 E2E 语法通过，5 项合并输入哈希匹配；前两次旧展示断言失败保留。真实 MySQL 生成 10/11 累计清单记录为 28/29 表、PASS，并清理容器。

schema/02 首轮已结束：35 个顶层 PASS、7 FAIL，原日志保留。其输入与当前仅 4 个历史/分界夹具文件不同；已审查这些修正，当前修复仍需窄复验，不能把首轮证据算作当前全部通过。#104 尚未接入；接入后须再次核对交互与 HTTP/迁移相交路径。ROLLED_BACK 临时总览归 #106，不作为本增量新缺陷。

实际输入：`/tmp/rcc-template-98-spec-merge.inputhash.json`，656 项源码及独立证据/保留检查；SHA-256：`4e23a2d4f9bbc151bcf807fda74b67ab7afc9898a0433561e5d7888d64b5a0ef`。
