# Spec review — #97

Review base: `12fda54fc3c2f74e09e7dbde13a6e723d6a98e33`. Reviewed `git diff <base>` plus the untracked `release_notifications*.go` and `web/e2e/release-notifications.cjs`; `<base>..HEAD` was empty during review. Sources: adjacent complete issue #97/#92 snapshots (body, comments, labels), `docs/specs/approval-roles-notifications/spec.md`, ADR-0026, and `web/DESIGN.md` for the result navigation seam.

**Findings: 0.** No missing requirement, scope creep, temporary exit omission, or suspicious implementation was found within this ticket.

- **AC-019:** The four hooks use the original business transaction. Publication/completion/rollback recipients come from the applicant and saved table decisions, deduplicate permanent IDs, and reuse the existing exclusion of the actor while retaining older unread results. Tests cover multiple tables, default ADMIN, removed role membership, unrelated viewers, aggregated read progress, and successful original-request replay. Formal account maintenance coverage also checks disabled historical participants after re-enable.
- **AC-019 / #97 “重新准备取消源单、创建替代草稿及旧单取消通知共同成功或失败”:** Failure injection and successful replay cover source cancellation, replacement identity, target transfer, request outcome, and applicant notification; the unsubmitted draft has no personal notification or approval-center row.
- **AC-020:** Tests inspect real MySQL values, record/table versions, executions, downstream refresh records, personal progress, state, targets, and request results across notification persistence failure, other storage failures, constraint failure, HTTP response loss, actual COMMIT acknowledgement loss, and terminal races. Their assertions distinguish confirmed failures from uncertain committed outcomes and prevent duplicate successful facts. The unchanged authorization/recovery boundaries continue to use the existing formal HTTP paths.
- **AC-019/020 browser seam:** `browser/initial/result.json` records all four lifecycle paths passing in Chromium, with no page errors. The script proves real UI result navigation, exact original key/body retries after reload, no automatic write retry, desktop/390px layout, keyboard access, and acknowledgement of the displayed sequence. Sampled mobile screenshots show readable original-order results and the retained reprepare error/retry state.

**Incremental review:** The two repaired regression fixtures preserve AC-020. Cancellation still passes real authentication and waits on the publication's actual authorization-lock connection; same-actor HTTP reads preserve the complete cancellation/business comparison, plus exactly one applicant reminder and no reviewer success reminder. Original-publication replay still compares every business field, checks current approval context against GET, and proves the stored request result unchanged byte-for-byte. Shared preparation leaves HTTP-loss and COMMIT-loss fault assertions independent.

Read `affected-regression.log` (18 PASS, two fixture failures), `regression-repaired.log` (four PASS), and `cancellation-notice-verified.log` (final strengthened cancellation PASS); disabled-participant maintenance also passed. Earlier failures remain retained. Final evidence manifests are the implementer's responsibility. No heavy tests or production mutations were run by this reviewer.
