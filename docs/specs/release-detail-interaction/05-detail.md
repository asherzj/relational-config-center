# T5：详情审阅、历史和操作恢复完整交互

状态：已交付；[#64](https://github.com/asherzj/relational-config-center/issues/64) 已关闭。实现与验证见[验收索引](../../verification/2026-09-09-release-detail-acceptance-index.md)。

## 父工单

Part of #59：https://github.com/asherzj/relational-config-center/issues/59

## 要构建什么

查看者按四阶段理解进展，在详情中审阅折叠差异、真实发布及回滚结果、最近历史和关联单；手机与键盘可完成操作，未确认请求保护输入并禁止本页面冲突动作。

## 验收标准

- [x] AC-012：MODIFY 隐藏确定不变字段，生成说明仍可见；ADD/DELETE 完整可读；操作范围仍是整单。
- [x] AC-013：默认展示可信实际结果；申请值、原发布结果和恢复结果明确区分；生成字段不由申请内容推测。
- [x] AC-014：阶段和动作真实，终止不伪装四步成功；默认最近五条可展开全部，关联跳转和复制反馈有效，页面无整体横向溢出。
- [x] AC-016：保留原内容与请求标识，阻止本页面冲突动作；后端竞争仍至多一个结果；明确拒绝经最新审阅才重建，不提前显示终止或释放。

## 验收用例

主要交付：AC-012, AC-013, AC-014, AC-016。采用父规格的 Given/When/Then，不引入未确认需求。

## 验证证据

Web 模块验证字段状态和千项分页、各状态动作/历史/结果切换、原请求恢复；真实浏览器桌面与 390px 验证主流程和长内容布局。与 T1–T4 使用本规格已冻结的接口联调后交付。

## 临时结构与退出条件

无。完成实现和本工单验证，修复此前中间改动；不得保留假成功、旧数据兼容或只声明未来测试。

## 阻塞于

- #62：重新准备的完整可操作路径（包含 T1 标题与人员）。
- #63：快速回滚的完整可操作路径（包含 T2 待完结与普通回滚）。

## 执行分配

一名全新上下文的独立 Sub Agent，在专属分支和可写 git worktree 中完成本工单实现、验证、评审与交付；模型 GPT-6 Astra，思考强度 high。主智能体负责依赖核验、接口协调与功能级证据汇总；本工单所需的后端、界面、测试和上下文更新均由本工单负责。
