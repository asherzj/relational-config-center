# T1：标题与人员信息贯通草稿和详情

状态：已交付；[#61](https://github.com/asherzj/relational-config-center/issues/61) 已关闭。实现与验证见[验收索引](../../verification/2026-09-09-release-detail-acceptance-index.md)。

## 父工单

Part of #59：https://github.com/asherzj/relational-config-center/issues/59

## 要构建什么

编辑者从所有建单入口保存可读标题，提交后冻结；普通查看者看到本单相关人员当前姓名且仍能查看永久 ID。

## 验收标准

- [x] AC-010：所有入口预填并保存合法标题；无效输入拒绝；旧版本不覆盖标题或明细；提交后冻结。
- [x] AC-011：显示当前姓名且历史仍归永久 ID；无法解析时 ID 兜底，不需要账号管理权限。

## 验收用例

主要交付：AC-010, AC-011。采用父规格的 Given/When/Then，不引入未确认需求。

## 验证证据

验证标题 Unicode 边界、必填、CAS、提交后不可编辑；真实 HTTP 验证查看权限和账号改名归属；Web 验证创建、批量删除、合并草稿及详情展示。

## 临时结构与退出条件

无。完成实现和本工单验证，修复此前中间改动；不得保留假成功、旧数据兼容或只声明未来测试。

## 阻塞于

无（可立即开始）。

## 执行分配

一名全新上下文的独立 Sub Agent，在专属分支和可写 git worktree 中完成本工单实现、验证、评审与交付；模型 GPT-5.6 Sol，思考强度 high。主智能体负责依赖核验、接口协调与功能级证据汇总；本工单所需的后端、界面、测试和上下文更新均由本工单负责。
