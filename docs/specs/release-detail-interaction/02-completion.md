# T2：发布成功保持待完结并保护目标

状态：已交付；[#60](https://github.com/asherzj/relational-config-center/issues/60) 已关闭。实现与验证见[验收索引](../../verification/2026-09-09-release-detail-acceptance-index.md)。

## 父工单

Part of #59：https://github.com/asherzj/relational-config-center/issues/59

## 要构建什么

普通发布成功后继续占用全部真实目标；其他有发布权限者可以通过明确确认完结，释放占用但不修改配置，完结后保留普通审批回滚入口。

## 验收标准

- [x] AC-001：原单保持已发布待完结；重叠提交被拒绝，不重叠提交正常。
- [x] AC-002：两类真实记录身份均继续受保护，完结或成功回滚才释放。
- [x] AC-007：完结只关闭快速回滚及释放目标，不改业务值或冒充分发；后续回滚仍需审批，原单不重新打开。

- [x] AC-008：完结后申请的普通审批回滚成功时，原单与反向结果均结束并释放目标，不允许连续反向发布。

## 验收用例

主要交付：AC-001, AC-002, AC-007, AC-008。采用父规格的 Given/When/Then，不引入未确认需求。

## 验证证据

真实 MySQL 验证已知、自增、删除目标、不重叠发布、完结权限及状态历史；Web 验证完结确认文案、取消和重复点击。必须将既有普通回滚路径迁移到完结后申请，并以完整审批回滚证明兼容当前新语义；快速回滚权限在 T4 交付。

## 临时结构与退出条件

无。完成实现和本工单验证，修复此前中间改动；不得保留假成功、旧数据兼容或只声明未来测试。

## 阻塞于

无（可立即开始）。

## 执行分配

一名全新上下文的独立 Sub Agent，在专属分支和可写 git worktree 中完成本工单实现、验证、评审与交付；模型 GPT-6 Astra，思考强度 xhigh。主智能体负责依赖核验、接口协调与功能级证据汇总；本工单所需的后端、界面、测试和上下文更新均由本工单负责。
